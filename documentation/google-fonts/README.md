# Google Fonts Notes

This directory is for curated Google Fonts submission inputs and current notes.
Generated readiness/report files from earlier agent passes were moved to
`documentation/archive/agent-generated-reports/google-fonts/`.

Active files:

- `ARTICLE.en_us.html`
- `DESCRIPTION.en_us.html`

Use `make test` for the current Fontspector `googlefonts` QA gate. Reusable
onboarding instructions live in `.agents/`.
